# Root env file (shared defaults for Docker Compose/backend)
# Copy this file to .env at the repository root and adjust values.

# Backend
PROVIDER_API_KEY=your-provider-key-here
DEFAULT_MODEL=command-r-plus
LOG_LEVEL=INFO
PORT=8000

# Frontend envs belong in frontend/.env (see frontend/.env.example)
