# Root env file (shared defaults). Service-level .env files can override these.
# Copy this file to .env at the repository root and adjust values.

# Backend
PROVIDER_API_KEY=your-provider-key-here
DEFAULT_MODEL=command-r-plus
LOG_LEVEL=INFO
PORT=8000

# Frontend
# This is the API base URL used by the browser app
VITE_API_URL=http://localhost:8000

